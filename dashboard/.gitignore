# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.vite

# local Vite-only overrides (release database paths are fixed by Rust)
.env

# a global gitignore with `lib/` (for Python venvs) would otherwise
# silently exclude the dashboard's core logic
!src/lib/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
